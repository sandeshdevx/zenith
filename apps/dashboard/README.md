# @zenith/dashboard (placeholder)

Counsellor dashboard — arrives in Phase 5 per ROADMAP.md.

Planned: React + Vite, magic-link auth + TOTP MFA, TanStack Query + WebSocket events, i18next. Receives only `{session_uuid, risk_tier, last_3_turns}` — never PII.

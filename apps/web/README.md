# @zenith/web (placeholder)

Anonymous user PWA — arrives in Phase 3 (text chat UI) per ROADMAP.md.

Planned: React + Vite + TypeScript, i18next with browser-language detection (`locales/{lang}.json`), Zustand in-memory store only — no conversation content ever touches localStorage/IndexedDB.
